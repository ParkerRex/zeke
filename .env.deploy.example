# Docker Registry (include trailing slash)
REGISTRY=ghcr.io/your-org/

# Remote Docker Hosts (optional, for SSH-based deploys)
# STAGING_DOCKER_HOST=ssh://ubuntu@staging.example.com
# PROD_DOCKER_HOST=ssh://ubuntu@prod.example.com
