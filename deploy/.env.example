# Compose defaults for the Zeke deployment stack.
# Copy to `.env` and adjust per environment before running `docker compose`.

ENVIRONMENT=staging
DASHBOARD_IMAGE=zeke-dashboard:staging
WEBSITE_IMAGE=zeke-website:staging
API_IMAGE=zeke-api:staging
ENGINE_IMAGE=zeke-engine:staging
